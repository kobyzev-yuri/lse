# Roadmap развития LSE Trading System

## Текущий статус ✅

Система полностью функциональна:
- ✅ База данных `lse_trading` с таблицами: `quotes`, `trade_kb`, `knowledge_base`, `portfolio_state`, `trade_history`
- ✅ `AnalystAgent` - анализ технических индикаторов + sentiment анализ новостей
- ✅ `ExecutionAgent` - исполнение сделок с учетом комиссий и стоп-лоссов
- ✅ `report_generator.py` - генерация отчетов по PnL и Win Rate
- ✅ Учет временного лага для макро-событий (72ч) vs обычных новостей (24ч)
- ✅ Взвешенный sentiment (больший вес новостям с упоминанием тикера)

---

## План развития (Roadmap)

### 🎯 Этап 1: Multi-Asset Rebalancing

**Цель**: Автоматическое управление рисками через ребалансировку портфеля

**Проблема**: Если позиция вырастает в 2 раза (например, SNDK), её доля в портфеле становится слишком большой, что увеличивает риск.

**Решение**:
1. Добавить метод `check_portfolio_allocation()` в `ExecutionAgent`
2. Определить максимальную долю одного актива в портфеле (например, 20%)
3. При превышении лимита:
   - Рассчитать избыточную позицию
   - Частично закрыть позицию (фиксация прибыли)
   - Записать в `trade_history` с `signal_type='REBALANCE_TRIM'`

**Технические детали**:
- Использовать текущие цены из `quotes` для расчета текущей стоимости позиций
- Формула: `current_value = quantity * current_price`
- Если `current_value / total_portfolio_value > max_allocation`:
  - Продать `excess_quantity = quantity - (max_allocation * total_portfolio_value / current_price)`
- Обновить `portfolio_state` после частичной продажи

**Файлы для изменения**:
- `execution_agent.py` - добавить метод `check_portfolio_allocation()`
- Вызывать после `check_stop_losses()` в `run_for_tickers()`

**Приоритет**: Высокий (управление рисками)

---

### 🎯 Этап 2: Visual Patterns (Object Detection)

**Цель**: Использование паттернов технического анализа для улучшения sentiment_score

**Идея**: Применить опыт работы с медицинскими изображениями к анализу графиков цен.

**Паттерны для распознавания**:
1. **Двойное дно (Double Bottom)** - бычий паттерн, добавляет +0.2 к sentiment
2. **Двойная вершина (Double Top)** - медвежий паттерн, добавляет -0.2 к sentiment
3. **Голова и плечи (Head and Shoulders)** - медвежий паттерн, добавляет -0.15 к sentiment
4. **Треугольник восходящий (Ascending Triangle)** - бычий паттерн, добавляет +0.15 к sentiment

**Решение**:
1. Создать модуль `pattern_detector.py`
2. Метод `detect_patterns(ticker, lookback_days=30)`:
   - Загружает последние N дней котировок из `quotes`
   - Анализирует последовательность High/Low/Close
   - Определяет паттерны через алгоритмы поиска локальных экстремумов
3. Интеграция с `AnalystAgent`:
   - В методе `get_decision()` после расчета sentiment добавить:
     ```python
     pattern_score = pattern_detector.get_pattern_score(ticker)
     adjusted_sentiment = weighted_sentiment + pattern_score
     ```

**Технические детали**:
- Использовать библиотеку `scipy.signal.find_peaks` для поиска локальных максимумов/минимумов
- Проверять условия паттернов:
  - Двойное дно: два локальных минимума примерно на одном уровне, между ними локальный максимум
  - Двойная вершина: два локальных максимума примерно на одном уровне, между ними локальный минимум
- Нормализовать цены перед анализом (z-score или min-max scaling)

**Файлы для создания**:
- `pattern_detector.py` - новый модуль для распознавания паттернов
- Обновить `analyst_agent.py` - интеграция pattern_score

**Приоритет**: Средний (улучшение качества сигналов)

---

### 🎯 Этап 3: Real-time API Integration

**Цель**: Переход от исторических данных к реальной торговле через демо-счет

**Текущее состояние**: Система работает на исторических данных из `yfinance`

**Решение**: Создать слой абстракции для работы с реальными API

**Варианты API**:
1. **Alpaca Markets** (рекомендуется для начала):
   - Бесплатный демо-счет
   - REST API и WebSocket для real-time данных
   - Python SDK: `alpaca-trade-api`
   - Хорошая документация

2. **Interactive Brokers (IBKR)**:
   - Более сложная настройка
   - Требует установки TWS/Gateway
   - Python библиотека: `ib_insync`
   - Больше возможностей для профессиональной торговли

**Архитектура решения**:
1. Создать модуль `data_provider.py` с интерфейсом:
   ```python
   class DataProvider:
       def get_current_price(self, ticker: str) -> float
       def get_historical_data(self, ticker: str, period: str) -> pd.DataFrame
       def place_order(self, ticker: str, side: str, quantity: int, order_type: str)
   ```

2. Реализации:
   - `YFinanceProvider` - текущая реализация (для бэктестинга)
   - `AlpacaProvider` - для реальной торговли
   - `IBKRProvider` - альтернативная реализация

3. Обновить `ExecutionAgent`:
   - Использовать `DataProvider` вместо прямых запросов к БД
   - Добавить проверку баланса через API
   - Реальные ордера вместо имитации

**Технические детали**:
- Использовать паттерн Strategy для переключения между провайдерами
- Сохранять все сделки в `trade_history` (как сейчас)
- Добавить поле `is_demo` в `trade_history` для различения демо и реальных сделок
- Реализовать rate limiting для API запросов

**Файлы для создания**:
- `data_provider.py` - абстрактный класс и реализации
- `alpaca_provider.py` - реализация для Alpaca API
- `config.env` - добавить API ключи (ALPACA_API_KEY, ALPACA_SECRET_KEY)

**Приоритет**: Высокий (переход к реальной торговле)

---

## Порядок реализации

### Фаза 1: Стабилизация и риск-менеджмент (1-2 недели)
1. ✅ Multi-Asset Rebalancing
2. Улучшение логирования и мониторинга
3. Добавление unit-тестов для критических компонентов

### Фаза 2: Улучшение сигналов (2-3 недели)
1. ✅ Visual Patterns Detection
2. Интеграция с AnalystAgent
3. Бэктестинг на исторических данных

### Фаза 3: Real-time интеграция (3-4 недели)
1. ✅ Real-time API Integration (Alpaca)
2. Настройка демо-счета
3. Тестирование на демо-счете
4. Мониторинг и оптимизация

---

## Метрики успеха

### Multi-Asset Rebalancing
- Максимальная доля одного актива в портфеле ≤ 20%
- Автоматическая фиксация прибыли при превышении лимита
- Снижение волатильности портфеля на 15-20%

### Visual Patterns
- Улучшение Win Rate на 5-10% за счет паттернов
- Точность распознавания паттернов ≥ 70%
- Интеграция с sentiment_score без конфликтов

### Real-time API
- Успешное подключение к демо-счету
- Исполнение сделок в реальном времени
- Латентность < 1 секунды от сигнала до исполнения
- Стабильная работа 24/7

---

## Риски и митигация

### Multi-Asset Rebalancing
- **Риск**: Слишком частая ребалансировка может увеличить комиссии
- **Митигация**: Добавить минимальный интервал между ребалансировками (например, 1 день)

### Visual Patterns
- **Риск**: Ложные срабатывания паттернов
- **Митигация**: Использовать комбинацию нескольких паттернов, добавить фильтр по объему торгов

### Real-time API
- **Риск**: Проблемы с подключением, таймауты API
- **Митигация**: Реализовать retry механизм, fallback на исторические данные при сбоях

---

## Зависимости

### Multi-Asset Rebalancing
- Текущие зависимости достаточны

### Visual Patterns
- `scipy>=1.10.0` - для поиска экстремумов
- `scikit-learn>=1.3.0` - для нормализации данных (опционально)

### Real-time API
- `alpaca-trade-api>=3.0.0` - для Alpaca
- Или `ib_insync>=0.9.86` - для Interactive Brokers
- `websocket-client>=1.6.0` - для real-time данных

---

## Документация

Каждый этап должен включать:
- README с инструкциями по настройке
- Примеры использования
- Unit-тесты
- Интеграционные тесты

---

## Контакты и поддержка

При возникновении вопросов или проблем:
1. Проверить логи в консоли
2. Проверить данные в БД через SQL запросы
3. Использовать `report_generator.py` для анализа результатов

---

**Последнее обновление**: 2026-02-13
**Версия системы**: 1.0.0

