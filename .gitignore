# Конфигурационные файлы с секретами
config.env
# config.env.lse — шаблон фрагмента для сервера (без секретов), хранится в репозитории
config.secrets.env
config.security.env
# Дампы БД (переносить на сервер через scp, не через git)
*.sql.gz

# Локальные данные о risk limits и capacity (не публикуем в git)
local/risk_limits.json
# Обученные модели CatBoost (игра 5m)
local/models/*.cbm
local/models/*.meta.json
local/*.risk.json
# Снимки отчёта анализатора (cron)
local/analyzer_snapshots/
risk_config/
risk_limits.json
risk_limits.env
*.risk.json
*.risk.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
dist/
*.egg-info/

# Логи
logs/
*.log
# Состояние watchdog (уже отправленные в Telegram ошибки)
.cron_watchdog_sent.json

# Кэш HTML-отчётов (closed/pending) для ссылок из Telegram
cache/

# Локальные бинарники туннеля (cloudflared), скачиваются scripts/run_tunnel.sh
scripts/bin/

# Cooldown для проактивных сигналов (cron)
.last_sndk_signal_sent
.last_signal_sent_*
# Локальные бэкапы БД (не в репозиторий)
lse_trading_backup_*.sql

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
=20.0

# Базы данных (если есть локальные)
*.db
*.sqlite

# Kerim Platform: Docker-образ (platform.tar) не в git — копировать на сервер в отдельную директорию (scp/rsync), там docker load / run
kerimsrv/*.tar

# tradenews — отдельный подпроект (свой репозиторий / релизы); не трекать в lse до явного решения
tradenews/
