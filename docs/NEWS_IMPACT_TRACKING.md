# Отслеживание влияния новостей на принятие решений

## Обзор

В `boss_dashboard.py` реализована детальная система отслеживания влияния новостей на принятие торговых решений. Это позволяет видеть, как именно анализ новостей и исторических паттернов влияет на финальные рекомендации.

## Структура данных

### NewsImpactTrace

Структура для детальной трассировки влияния новостей:

```python
@dataclass
class NewsImpactTrace:
    base_recommendation: str          # Рекомендация до учета новостей
    final_recommendation: str         # Финальная рекомендация после учета новостей
    recommendation_changed: bool      # Была ли изменена рекомендация
    change_reason: Optional[str]      # Причина изменения
    news_count: int                  # Количество новостей за 7 дней
    similar_events_count: int        # Количество похожих исторических событий
    impact_pattern: Optional[str]    # 'POSITIVE', 'NEGATIVE', 'NEUTRAL'
    impact_confidence: Optional[float] # Уверенность в паттерне (0.0-1.0)
    historical_avg_change: Optional[float] # Среднее изменение цены (%)
    sentiment_score: Optional[float]  # Взвешенный sentiment из AnalystAgent
```

## Как это работает

### 1. Базовая рекомендация

Сначала формируется базовая рекомендация на основе:
- Режима VIX (LOW_FEAR / NEUTRAL / HIGH_PANIC)
- Корреляции с сектором
- Технических индикаторов
- Выбранной стратегии

### 2. Анализ новостей

Затем анализируются новости:
- **Количество новостей** за последние 7 дней
- **Sentiment анализ** (взвешенный sentiment из AnalystAgent)
- **Поиск похожих событий** через Vector KB
- **Исторические паттерны** (как рынок реагировал на похожие новости)

### 3. Корректировка рекомендации

Если анализ новостей показывает значимое влияние:

- **POSITIVE паттерн** (рост >2% с уверенностью >50%):
  - `HOLD` → `STRONG_BUY`
  - Причина: "Исторический анализ показал рост X% после похожих событий"

- **NEGATIVE паттерн** (падение >2% с уверенностью >50%):
  - `STRONG_BUY` → `LIMIT_ORDER`
  - Причина: "Исторический анализ показал падение X% после похожих событий"

- **Сильный sentiment** (>0.5 или <-0.5):
  - Может усилить или ослабить рекомендацию
  - Причина: "Сильный положительный/отрицательный sentiment"

### 4. Детальная трассировка

Вся информация сохраняется в `NewsImpactTrace`:
- Базовая и финальная рекомендации
- Флаг изменения
- Причина изменения
- Все метрики анализа новостей

## Формат вывода в дашборде

При запуске `boss_dashboard.py` для каждого тикера выводится:

```
📰 News Impact Analysis:
   News count (7d): 12
   Similar historical events: 10
   Sentiment score: 0.650 (positive)
   Historical pattern: 📈 POSITIVE (confidence: 75%)
   Historical avg price change: +3.2%
   Base recommendation (before news): HOLD
   Final recommendation (after news): STRONG_BUY
   ⚠️  RECOMMENDATION CHANGED due to news analysis!
   Reason: Исторический анализ показал рост 3.2% после похожих событий
```

Или если рекомендация не изменилась:

```
📰 News Impact Analysis:
   News count (7d): 5
   Similar historical events: 3
   Sentiment score: 0.200 (neutral)
   Base recommendation (before news): STRONG_BUY
   Final recommendation (after news): STRONG_BUY
   ✓ Recommendation unchanged (news analysis confirmed base decision)
```

## Примеры использования

### Пример 1: Новости усилили рекомендацию

**Базовая рекомендация:** `HOLD` (на основе VIX и технических индикаторов)

**Анализ новостей:**
- 15 новостей за 7 дней
- Sentiment: 0.7 (положительный)
- Найдено 8 похожих событий
- Исторический паттерн: POSITIVE (75% уверенность)
- Среднее изменение цены: +4.1%

**Результат:**
- Финальная рекомендация: `STRONG_BUY`
- **RECOMMENDATION CHANGED**
- Причина: "Исторический анализ показал рост 4.1% после похожих событий"

### Пример 2: Новости подтвердили решение

**Базовая рекомендация:** `STRONG_BUY` (на основе технических сигналов)

**Анализ новостей:**
- 8 новостей за 7 дней
- Sentiment: 0.4 (слабо положительный)
- Найдено 5 похожих событий
- Исторический паттерн: POSITIVE (60% уверенность)
- Среднее изменение цены: +2.5%

**Результат:**
- Финальная рекомендация: `STRONG_BUY`
- **Recommendation unchanged**
- Новости подтвердили базовое решение

### Пример 3: Новости ослабили рекомендацию

**Базовая рекомендация:** `STRONG_BUY` (на основе технических сигналов)

**Анализ новостей:**
- 20 новостей за 7 дней
- Sentiment: -0.6 (отрицательный)
- Найдено 12 похожих событий
- Исторический паттерн: NEGATIVE (80% уверенность)
- Среднее изменение цены: -3.8%

**Результат:**
- Финальная рекомендация: `LIMIT_ORDER`
- **RECOMMENDATION CHANGED**
- Причина: "Исторический анализ показал падение 3.8% после похожих событий"

## Технические детали

### Интеграция с AnalystAgent

`boss_dashboard.py` получает `sentiment_score` из `AnalystAgent.get_decision_with_llm()`:
- Используется `sentiment_normalized` (шкала -1.0 до 1.0)
- Если доступен только `sentiment` (шкала 0.0-1.0), автоматически конвертируется

### Интеграция с Vector KB

Поиск похожих событий выполняется через:
- `analyze_news_impact_pattern()` - использует последнюю новость как контекст
- `vector_kb.search_similar()` - семантический поиск в `trade_kb`
- `news_impact_analyzer.aggregate_patterns()` - анализ исходов похожих событий

### Пороги влияния

Рекомендация изменяется только если:
- Уверенность в паттерне > 50%
- Историческое изменение цены > 2% (для POSITIVE) или < -2% (для NEGATIVE)
- Или sentiment > 0.5 (положительный) / < -0.5 (отрицательный)

## Логирование

Все изменения рекомендаций логируются в reasoning:
- Добавляется префикс 📈 для положительных паттернов
- Добавляется префикс 📉 для отрицательных паттернов
- Добавляется префикс ➡️ для нейтральных паттернов

## Ограничения

1. **Требуются данные об исходах:** Для анализа паттернов нужны данные в `outcome_json` колонке `trade_kb`
2. **Минимальная выборка:** Для надежных выводов рекомендуется минимум 5-10 похожих событий
3. **Контекст поиска:** Используется последняя новость из `knowledge_base`

## Связанные файлы

- `boss_dashboard.py` - основной дашборд с отслеживанием влияния
- `services/vector_kb.py` - поиск похожих событий
- `services/news_impact_analyzer.py` - анализ исходов событий
- `analyst_agent.py` - расчет sentiment и базовых рекомендаций
