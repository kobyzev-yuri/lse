# Стратегии, LLM и отчёты: подходы и реализация

Документ фиксирует подходы к стратегиям, использованию LLM при принятии решений о входе и к отчётам в Telegram-боте.

---

## 1. Стратегии в системе

### 1.1 Источники сделок (кто открыл/закрыл)

| Стратегия   | Описание |
|------------|----------|
| **GAME_5M** | Игра 5m: крон по тикерам из `GAME_5M_TICKERS`, интрадей (вход/выход по 5m, тейк/стоп). В `/pending` для тикеров, убранных из списка, показывается «5m вне» — крон по ним не управляет. |
| **Portfolio** | Портфельный цикл (`trading_cycle_cron`, ExecutionAgent). Сделки по сигналу AnalystAgent по списку MEDIUM/LONG. Если StrategyManager не вернул имя стратегии, в БД пишется «Portfolio». Выходы: `ExecutionAgent.check_stop_losses()` — сейчас рабочий автоматический выход по тейку; stop-loss портфеля выключен флагом `PORTFOLIO_STOP_LOSS_ENABLED=false`. Подробный алгоритм: [PORTFOLIO_GAME.md](PORTFOLIO_GAME.md). |
| **Manual** | Ручные команды `/buy` и `/sell` в боте. |

Подробнее: команда бота `/strategies`, портфельный алгоритм в [PORTFOLIO_GAME.md](PORTFOLIO_GAME.md).

### 1.2 Стратегии StrategyManager (портфельный цикл)

Выбираются по режиму рынка (волатильность, sentiment, гэп) на **дневных** данных (`get_last_5_days_quotes`, SMA/вола 5 и 20 дней) и **новостях из KB** (`get_recent_news`, взвешенный sentiment, insight в стратегиях). В `technical_data` передаются **^VIX** (`vix_value`, `vix_regime`): при **VIX ниже 20** чуть выше порог для ветки Volatile Gap (меньше ложных «панических» режимов на фоне гео-шума); при **LOW_FEAR** или **VIX ниже 18** чуть шире окно для Momentum по отношению волатильности. Точная очередность выбора и примеры `Volatile Gap` / `Geopolitical Bounce` описаны в [PORTFOLIO_GAME.md](PORTFOLIO_GAME.md).

- **Momentum** — низкая волатильность + положительный sentiment.
- **Mean Reversion** — высокая волатильность + нейтральный sentiment.
- **Volatile Gap** — очень высокая волатильность + гэп или экстремальный sentiment.
- **Geopolitical Bounce** — резкое падение предыдущей сессии (≥2%), отскок long.
- **Neutral** — fallback, когда ни одна стратегия не подошла; консервативный HOLD.

Strategy-level параметры `PORTFOLIO_<STRATEGY>_TAKE_PROFIT_PCT` не зависят от тикера в `config.env`: выбранная стратегия фиксирует свой процент тейка в BUY, а закрытие ждёт достижения этого процента. Тикерные переопределения возможны через `strategy_parameters`, если они заведены для `TICKER:<ticker>`.

**Portfolio** vs **Neutral**: Portfolio — метка по умолчанию при пустом имени стратегии; Neutral — явно выбранная стратегия (режим не определён, удержание).

**Связь с быстрой игрой (5m):** те же принципы учёта новостей, кластера и (в LLM) гео-блока; горизонт решения — **день/несколько дней**, а не 5m-бар. Прогноз 30/60/120 мин в промпте — вспомогательный контекст, не замена дневной картины (см. п. 8 системного промпта в `llm_service.get_entry_decision_system_prompt`).

---

## 2. LLM при принятии решения о входе

### 2.1 Промпт

- Системный промпт задаёт роль (финансовый аналитик) и формат ответа (JSON: decision, confidence, reasoning, risks, key_factors).
- В пользовательское сообщение передаются: тикер, технические данные (цена, SMA, волатильность, RSI, технический сигнал, при наличии — **^VIX**), sentiment, новости (топ-5), при необходимости — контекст премаркета, кластер, гео-блок из новостей.

Получить актуальный текст промпта: команда бота `/prompt_entry`. Код: `services/llm_service.py` — `get_entry_decision_system_prompt()`, `analyze_trading_situation()`.

### 2.2 Учёт стратегии в промпте

- В промпт передаётся **выбранная стратегия** (StrategyManager) и **её сигнал** (BUY/HOLD/SELL).
- Строка в промпте: «Контекст стратегии: выбранная стратегия — {name}, её сигнал — {signal}. Учти при итоговой рекомендации (можешь согласиться или скорректировать).»

Реализация: `analyst_agent.get_decision_with_llm()` передаёт `strategy_name` и `strategy_signal` в `llm_service.analyze_trading_situation()`.

### 2.3 Учёт исходов по стратегиям (история)

- Собирается **агрегированная статистика** по закрытым сделкам за последние 180 дней: по каждой стратегии (entry_strategy) — число сделок, число в плюс, доля в плюс (%), суммарный PnL ($).
- Эта строка передаётся в LLM как `strategy_outcome_stats`: «История по стратегиям (закрытые сделки): … Учти исходы в похожих ситуациях при рекомендации.»

Реализация: `report_generator.get_strategy_outcome_stats(engine, limit_days=180)`, вызов из `analyst_agent.get_decision_with_llm()`, параметр `analyze_trading_situation(..., strategy_outcome_stats=...)`.

Ограничение: «похожесть» ситуаций пока не учитывается — передаётся общая статистика по всем закрытым сделкам за период. Фильтрация по режиму (волатильность/sentiment на момент входа) может быть добавлена при появлении сохранения режима при открытии сделки.

### 2.4 Финальное решение

- Если LLM вернул BUY или STRONG_BUY — берётся решение LLM.
- Иначе используется **base_decision** от выбранной стратегии (StrategyManager).

### 2.5 Где используется LLM для прогноза, где нет

Чтобы не путать пользователя разными исходами (с LLM и без), в **Telegram-боте** все команды, показывающие прогноз (решение BUY/HOLD/STRONG_BUY), используют один путь — **с LLM**:

| Команда | Использует LLM (портфель) |
|---------|---------------------------|
| `/signal <ticker>` | да (AnalystAgent) |
| `/recommend [ticker]` | да (тот же контур, что prompt_entry portfolio) |
| `/ask` (вопрос с тикером, анализ) | да |
| `/prompt_entry` (portfolio, тикер) | да; game5m — решение по правилам, опц. LLM-новости |

Бот создаёт `AnalystAgent(use_llm=True)`. Решение пересматривается моделью по полному контексту (новости, риски, геополитика).

**Без LLM** (только стратегия + тех. сигнал + sentiment, без пересмотра моделью):

| Место | Причина |
|-------|---------|
| **Дашборд** (`/dashboard`, `send_dashboard_cron`) | Свой `AnalystAgent(use_llm=False)` и `get_decision(ticker)` — экономия API при частой рассылке по крону. |
| **Backtest** | По умолчанию `use_llm=False` для скорости. |

Разница: с LLM модель может скорректировать решение (например, HOLD при геополитическом риске при открытых позициях); без LLM остаётся только базовое решение стратегии.

**Управление:** параметр **USE_LLM** в `config.env` (или в глобальных параметрах БД `strategy_parameters`, entity=GLOBAL). При `USE_LLM=false` LLM не применяется (бот и trading_cycle используют только стратегию + тех. сигнал + sentiment). Иначе — применяется. Функция `config_loader.get_use_llm_for_analyst(engine=None)` — единая точка чтения (если передан engine, значение может браться из БД).

### 2.6 Корреляция и контекст по пулу

Актуальная схема — **кластерная**, а не «MU как специальный бенчмарк для всех».

Для портфельного LLM-пути `AnalystAgent.get_decision_with_llm()` пытается собрать `cluster_context`: список тикеров и 30-дневную матрицу корреляций. В промпт попадает блок вида:

```text
Кластер тикеров: ...
Корреляция этого тикера с другими (30 дн.): ...
По остальным тикерам кластера (цена, тех. сигнал): ...
```

Если портфельный cron идёт по списку тикеров, туда также могут попасть уже полученные в этом запуске сигналы по предыдущим тикерам (`other_signals`). Это нужно для контекста риска: не дублировать один и тот же highly-correlated trade и осторожнее относиться к расхождению сигналов.

Старый блок `corr_with_benchmark` / `benchmark_signal` с жёстким бенчмарком **MU** остался в коде только как fallback, если `cluster_context` не собрался, и только для портфельного LLM-пути по тикеру, отличному от `MU`. Это не правило GAME_5M и не основная текущая схема.

Для игры 5m используется отдельный контур корреляций: торгуемые тикеры (`GAME_5M_TICKERS`) + контекст (`GAME_5M_CORRELATION_CONTEXT`). Матрица считается по объединённому списку; в отчётах и LLM-блоках по каждому торгуемому тикеру показывается корреляция с остальными, а не особая зависимость от `MU`. См. п. 2.8.

### 2.7 Концепция: техническая рекомендация, LLM и команды вывода

**Два уровня решения:**

1. **Техническая рекомендация** — результат правил и стратегии без LLM: StrategyManager (портфель) даёт base_decision по волатильности/sentiment; игра 5m — правила по RSI, импульсу, новостям KB. Это «сырой» сигнал.
2. **Итоговая рекомендация** — то, что идёт в сделки и показывается пользователю. При **USE_LLM=true** (портфель): LLM получает техданные, стратегию, новости, кластер и может согласиться или скорректировать; итог = ответ LLM или base_decision. При **USE_LLM=false** итог = техническая рекомендация. В игре 5m LLM не формирует единый промпт «вход/выход» — решение по правилам и контексту (KB, опционально LLM-новости для тона).

Таким образом, **LLM учитывает техническую рекомендацию** (видит её в промпте и может скорректировать); без LLM пользователь видит только техническую рекомендацию.

**Связь команд бота (одна цепочка решений — разные формы вывода):**

| Команда | Игра | Что вызывает | Формат вывода |
|--------|------|---------------|----------------|
| **/signal &lt;ticker&gt;** | Портфель | AnalystAgent.get_decision_with_llm(ticker) — один тикер, без кластера | Краткий анализ: цена, RSI, решение, стратегия, sentiment (итоговая рекомендация по одному тикеру) |
| **/recommend [ticker]** | Портфель | То же решение, что prompt_entry portfolio: get_decision_with_llm (с кластером при «без тикера») | Текст рекомендации: когда входить, стоп/тейк, позиция (итоговая рекомендация; один тикер или весь кластер) |
| **/recommend5m [ticker] [days]** | 5m | get_decision_5m (правила + KB + опц. LLM-новости) — тот же контур, что prompt_entry game5m | Текст рекомендации по 5m: сигнал, цена, RSI, стоп/тейк (итоговая рекомендация; один тикер или кластер 5m) |
| **/prompt_entry [portfolio\|game5m\|ticker]** | Портфель или 5m | То же решение, что recommend/recommend5m и кроны | **Отчёт для человека:** контекст + ответ/обоснование по каждому тикеру; для портфеля в отчёте также **промпт для LLM** (system, user, ответ). Не «рекомендация», а «как получено решение». |

Итого: **recommend** и **recommend5m** — это **итоговая рекомендация** (короткое сообщение: что делать). **prompt_entry** — **шаблон/отчёт** (как получено решение: контекст и ответ). **signal** — итоговая рекомендация по одному тикеру в контуре портфеля (анализ + решение). Все они опираются на одну и ту же цепочку решений по выбранной игре; разница только в форме вывода (краткий текст vs полный отчёт/промпт).

### 2.8 Фокус на игре 5m и корреляция «все со всеми»

**Цель агента:** не прогноз ради прогноза, а подсказка/уведомление об ожидаемом росте → открытие позиции и извлечение прибыли при закрытии. **Приоритет:** прогнозы для вступления в игру — повышенное внимание; играет трейдер, ему нужна информация по возможности раньше (крон 5m каждые 5 мин, опционально премаркет-сводка с блоком «Прогноз на вступление (5m)»). Портфельная игра пока не в фокусе.

**Целевые стоки для 5m (daily):**
- Лидеры: **SNDK**, **NBIS** — высокий дневной рост, особенно после откатов (откат от 52w high >15–20% как индикатор отскока).
- AI bottlenecks: **ASML**, **MU** — тренд вверх, откат вниз как сигнал возможного роста (реже 10%/день, 5%/день часто).
- Волатильные: **LITE**, **CIEN** — могут дать 15%/день или долго около нуля; откат от максимумов как индикатор подпрыга.

**Фон (контекст, не торгуем daily):** MSFT, META, AMZN — индикатор общего настроения рынка; по ним daily торговать бессмысленно. Плюс индикаторы: NVDA, SMH, QQQ, TLT, ^VIX, нефть (CL=F), forex (GBPUSD=X и др.) — для корреляции и метрик.

**Конфиг (`config.env`):**
- **TICKERS_FAST** / **GAME_5M_TICKERS** — торгуемые тикеры 5m: `SNDK,NBIS,ASML,MU,LITE,CIEN`.
- **GAME_5M_CORRELATION_CONTEXT** — тикеры для матрицы корреляции «все со всеми»: фон (MSFT, META, AMZN) + индикаторы (NVDA, SMH, QQQ, TLT, ^VIX, CL=F, GBPUSD=X). Решения и входы только по тикерам игры 5m; корреляция считается по объединённому списку (игра + контекст) и показывается в отчётах `/prompt_entry game5m`, `/recommend5m`.
- **GAME_5M_TAKE_PROFIT_MIN_PCT** — минимальный импульс (%), с которого тейк считается от импульса 2ч; иначе тейк из конфига. Рекомендация: 4% для SNDK, NBIS, ASML, MU, LITE, CIEN.

Код: `services/ticker_groups.py` — `get_tickers_game_5m()`, `get_game_5m_correlation_context()`, `get_tickers_for_5m_correlation()`.

---

## 3. Отчёты в Telegram-боте

### 3.1 История сделок (`/history`)

- Маркеры по **фактическому PnL**, а не по signal_type: выход в плюс (цена выхода ≥ цены входа) → синий кружок (тейк), в минус → красный (стоп).
- Легенда: 🟢 Вход · 🔵 Выход в плюс · 🔴 Выход в минус.

### 3.2 Закрытые позиции (`/closed`)

- Колонки **Entry** и **Exit** — стратегия открытия (первый BUY) и стратегия закрытия (SELL), чтобы различать случаи «открыли в GAME_5M, закрыли в Manual».
- Реализация: `report_generator.TradePnL` — поля `entry_strategy`, `exit_strategy`; в `compute_closed_trade_pnls` ведётся `position_open_strategy` при первом BUY по позиции.

### 3.3 Открытые позиции (`/pending`)

- Колонка **Strategy** — стратегия последнего BUY по позиции; для тикеров, убранных из игры 5m, показывается «5m вне» (крон не управляет).
- Колонки **Now** и **P/L** — текущая цена (последняя close из `quotes`) и нереализованный P/L (% и $).
- Цена и P/L считаются по последней доступной дневной close в БД.

### 3.4 Переназначение стратегии

- Команда `/set_strategy <ticker> <стратегия>` и скрипт `scripts/set_position_strategy.py`: обновляют `strategy_name` у **последнего BUY** по тикеру в `trade_history`.
- Используется для позиций «5m вне» (например GC=F после вывода из GAME_5M): переназначение на Manual или Portfolio.

---

## 4. Параметры конфигурации и возможность менять их без перезапуска

### 4.1 Как сейчас читаются параметры

| Где | Параметры | Когда читаются |
|-----|-----------|----------------|
| **config.env** | Все переменные | При каждом вызове `get_config_value()` заново читается файл (кэша нет). |
| **game_5m** | GAME_5M_STOP_LOSS_PCT, GAME_5M_TAKE_PROFIT_PCT, GAME_5M_MAX_POSITION_DAYS, GAME_5M_TAKE_PROFIT_MIN_PCT, GAME_5M_STOP_TO_TAKE_RATIO, GAME_5M_STOP_LOSS_MIN_PCT | При каждом вызове `get_strategy_params()`, `_effective_take_profit_pct()`, `should_close_position()` — т.е. при каждом запуске крона. Изменение config.env на диске подхватится следующим запуском крона (но не бота, если крон — отдельный процесс). |
| **execution_agent** | `PORTFOLIO_STOP_LOSS_ENABLED`, `PORTFOLIO_EXIT_ONLY_TAKE`, `STOP_LOSS_LEVEL`, `SANDBOX_SLIPPAGE_SELL_PCT` | `PORTFOLIO_STOP_LOSS_ENABLED=false` отключает портфельный stop-loss; при этом strategy-level `*_STOP_LOSS_PCT` могут сохраняться в BUY, но не закрывают позицию. `SANDBOX_SLIPPAGE_SELL_PCT` читается при вызове `_get_slippage_sell_pct()`. |
| **StrategyManager** | Пороги выбора стратегии (волатильность 1.5x, sentiment ±0.6, гэп 3%) | Зашиты в `__init__` менеджера, не из config. Создаётся один раз при старте AnalystAgent. |
| **ticker_groups** | GAME_5M_TICKERS, TICKERS_FAST, TICKERS_MEDIUM, TICKERS_LONG | При каждом вызове `get_tickers_game_5m()` и др. — снова читается config. |

Итог: часть параметров (GAME_5M_*, тикеры, portfolio strategy take/stop keys) по факту читается при обращении к ним, но источник один — config.env на диске. Портфельный cron стартует отдельным процессом и подхватывает новые значения на следующем запуске; долгоживущий web/API процесс лучше перезапускать после сохранения параметров. Пороги выбора `StrategyManager` пока зашиты в коде.

### 4.2 Имеет ли смысл вариации параметров

Да, имеет смысл:

- **Быстрая подстройка** — ужесточить стоп или тейк на период волатильности без деплоя.
- **Эксперименты и A/B** — разные наборы параметров для части тикеров или времени.
- **Агент/пользователь** — LLM или пользователь предлагает «поднять тейк до 6%»; применение без перезапуска и без правки файла на сервере.

### 4.3 Возможный подход: runtime-переопределения

- **Хранилище переопределений** — таблица в БД (например `config_overrides`: key, value, updated_at). При чтении параметра: сначала проверять переопределение, при отсутствии — `get_config_value()` (config.env).
- **Команда бота** — например `/config get [KEY]` и `/config set KEY VALUE` (для доверенных пользователей), чтобы просматривать и менять переопределения. Список ключей можно ограничить (только GAME_5M_*, STOP_LOSS_LEVEL после выноса в config и т.д.).
- **Агент** — может получать текущие значения (из конфига + overrides) и предлагать изменения в ответе; фактическое применение — через `/config set` или отдельный подтверждаемый шаг.

Тогда параметры, которые уже читаются в рантайме (GAME_5M_*, тикеры), начнут учитывать overrides без перезапуска; константы в коде (STOP_LOSS_LEVEL, пороги StrategyManager) для такой схемы нужно вынести в чтение из config/overrides при каждом использовании.

### 4.4 Предпочтительная модель: параметры и история в БД (обсуждение)

Целевая модель: **в базе данных хранятся параметры стратегий и история их изменений**, чтобы потом учитывать эффективность в зависимости от того, какие значения стояли в тот или иной период.

- **Параметры** — текущие значения (тейк/стоп, пороги выбора стратегии, макс. срок позиции и т.д.) хранятся в БД. При принятии решений и при работе крона читается актуальный набор из БД (с fallback на config.env при отсутствии записи).
- **История изменений** — каждое изменение параметра фиксируется: кто изменил (оператор / агент), когда, какое значение было и стало, при необходимости — причина или контекст (например, «повышенная волатильность»). Это даёт временную шкалу: «с такой-то даты по такую-то действовали такие параметры».
- **Учёт эффективности** — по закрытым сделкам и по периодам можно сопоставлять: при каких параметрах какая была доходность, просадка, винрейт. Оператор или агент меняет параметры в зависимости от трендов и режимов рынка; история в БД позволяет оценить, насколько удачными были эти изменения.

Таким образом, БД выступает единым источником правды и по текущим настройкам, и по их эволюции, что нужно для анализа эффективности стратегий при изменениях, вносимых оператором или агентом.

---

## 5. Связанные файлы и команды

| Компонент | Файлы | Команды бота |
|-----------|--------|---------------|
| Стратегии (описание) | `strategies/`, `strategy_manager.py` | `/strategies` |
| Итоговая рекомендация (портфель) | `analyst_agent.py`, `llm_service.py` | `/signal`, `/recommend` |
| Итоговая рекомендация (5m) | `services/recommend_5m.py` | `/recommend5m` |
| Отчёт / промпт (как получено решение) | `services/telegram_bot.py` (_build_prompt_entry_*_html) | `/prompt_entry` (см. разд. 2.7) |
| Статистика исходов | `report_generator.get_strategy_outcome_stats` | — |
| Отчёты по сделкам | `report_generator.py`, `services/telegram_bot.py` | `/history`, `/closed`, `/pending`, `/set_strategy` |
| Параметры конфига | `config_loader.py`, `services/game_5m.py`, `execution_agent.py` | возможная команда `/config` (см. п. 4) |

---

*Документ актуален по состоянию реализации на момент коммита.*
