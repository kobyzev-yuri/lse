# Отчёт: торговый день GAME_5M — 07.05.2026 (прод)

Сформировано: **2026-05-08** (анализатор на прод-БД в контейнере `lse-bot`, окно **3 календарных дня** по закрытым сделкам `GAME_5M`; в тексте ниже — фокус на **07.05.2026** по факту сессии и строк `trade_history`).

## 1. Сводка дня по `trade_history` (стратегия GAME_5M)

Все записи за **07.05.2026** (времена как в БД на VM):

| id | ts | ticker | side | signal_type | price | qty |
|----|-----|--------|------|-------------|-------|-----|
| 575 | 07.05 16:30 | NBIS | SELL | TIME_EXIT_EARLY | 186.27 | 52 |
| 576 | 07.05 16:30 | CIEN | SELL | TIME_EXIT_EARLY | 550.55 | 17 |
| 577 | 07.05 17:55 | SNDK | SELL | TIME_EXIT_EARLY | 1356.28 | 21 |
| 578 | 07.05 21:10 | NBIS | BUY | BUY | 183.81 | 54 |
| 579 | 07.05 21:30 | NBIS | SELL | TAKE_PROFIT | 182.99 | 54 |

**Смысл дня:** три крупных ранних выхода по **TIME_EXIT_EARLY** (NBIS, CIEN, SNDK); затем новый вход NBIS и закрытие с типом **TAKE_PROFIT** по цене **ниже** входа (известный кейс: решение по intrabar high, запись выхода по **close** бара; отдельно на этот день повлиял снятый merge с «котировкой из БД» — см. §4).

## 2. Анализатор эффективности (3 дня, GAME_5M, `include_trade_details`)

Запуск:

`python3 scripts/analyze_trade_effectiveness_weekly.py --days 3 --strategy GAME_5M --include-trade-details`

Ключевые строки сводки (весь период 3 дн., не только 07.05):

- **Сделок:** 15 | **Win rate:** 53.33% | **Net PnL:** ≈ **+$973.82**
- **TIME_EXIT_EARLY (сводка):** 6 сделок с типом выхода, с OHLC в отчёте — 3; whipsaw 1h — 2 (66.67%); приоритет правок — ветка **`stale_reversal`** (`GAME_5M_STALE_REVERSAL_*`); есть предложения high/medium по порогам.
- **Крупнейшие минусы в окне:** CIEN #576 −4.58%, NBIS #549 −4.02%, SNDK #577 −3.27%, SNDK #565 −2.92% (часть — хвост **06.05**, логически связан с **07.05** по удержанию SNDK).
- **Recovery ML (офлайн):** модель ok; сценарий defer по τ — для ревью D4a/D4b, на исполнение не влияет.

Детальный текст анализатора (top losses, эвристики `GAME_5M_*`, CatBoost) — в логе прогона на VM; при необходимости повторить команду выше и сохранить вывод в файл.

## 3. SNDK и докупки (связь с убытком #577)

За **06–07.05** по SNDK на VM фиксировалась цепочка: после **TIME_EXIT_EARLY** (#565) снова набор позиции **тремя** BUY (в т.ч. докупка **~1410**), затем выход **21** лотом по **TIME_EXIT_EARLY** (#577) — средняя цена входа около **$1402**, выход **$1356**, **≈−3.27%** на нетто.

**Операционная правка на VM (08.05.2026):**  
`GAME_5M_ALLOW_PYRAMID_BUY=false` (без изменений), **`GAME_5M_ALLOW_PYRAMID_IF_NOT_HANGER=false`** (раньше было `true` — разрешало докуп при «не висяк»). После правки выполнен **`docker compose restart lse postgres`**.

Имеет смысл не затирать `config.env` на VM деплоем без того же значения в шаблоне/пайплайне.

## 4. Код / риск ложного тейка по котировке

В репозитории: коммит **`b23ccca`** — убран merge `get_latest_prices` в цену для **`should_close_position`** (ложный **TAKE_PROFIT** при спайке котировки vs 5m OHLC, кейс NBIS **579**). Для эффекта в проде нужен **деплой образа** `lse-bot` с этим коммитом; правка только `config.env` на хосте старый код не заменит.

## 5. Рекомендации на ближайший цикл

1. Прогон анализатора **раз в 1–2 недели** и точечный разбор `time_exit_early_action_summary` / `stale_reversal` vs фактические `exit_detail` в `context_json` SELL.
2. Удержать **`GAME_5M_ALLOW_PYRAMID_IF_NOT_HANGER=false`**, пока не будет отдельного решения о докупках после **TIME_EXIT_EARLY**.
3. После деплоя **`b23ccca`**: контроль по логам `HANGER_TACTIC` (поля `price_5m`, `quote`, `price_for_check`) и по SELL NBIS без отрицательного PnL при «логически недостигнутом» тейке по high.
