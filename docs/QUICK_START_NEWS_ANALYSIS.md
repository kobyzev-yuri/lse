# Быстрый старт: Анализ новостей и исходов событий

## 🚀 Первоначальный запуск (один раз)

### Шаг 1: Анализ исходов для существующих событий

```bash
# Анализировать все события старше 7 дней
python scripts/analyze_event_outcomes_cron.py
```

**Что делает:**
- Находит события в `trade_kb`, которым прошло 7+ дней
- Анализирует изменение цены после события
- Сохраняет результаты в `outcome_json`

**Результат:** Заполняет `outcome_json` для существующих событий, что позволит системе анализировать исторические паттерны.

---

### Шаг 2: Добавление sentiment к новостям без sentiment

```bash
# Добавить sentiment к новостям за последние 7 дней (ограничить до 50 для экономии)
SENTIMENT_DAYS_BACK=7 SENTIMENT_LIMIT=50 python scripts/add_sentiment_to_news_cron.py
```

**Требования:**
- `USE_LLM=true` в `config.env`
- Настроенный LLM сервис

**Что делает:**
- Находит новости из RSS и NewsAPI без sentiment
- Рассчитывает sentiment через LLM
- Обновляет `sentiment_score` в `knowledge_base`

**Результат:** Увеличивает покрытие sentiment анализом, что улучшает взвешенный sentiment в `AnalystAgent`.

---

## 📅 Настройка автоматизации (cron jobs)

Добавьте в crontab (`crontab -e`):

```bash
# Порядок важен! Запускаются последовательно:

# 1. Получение новостей (существующий)
0 1 * * * cd /path/to/lse && python scripts/fetch_news_cron.py >> logs/fetch_news.log 2>&1

# 2. Добавление sentiment к новостям (НОВЫЙ)
0 2 * * * cd /path/to/lse && python scripts/add_sentiment_to_news_cron.py >> logs/add_sentiment_to_news.log 2>&1

# 3. Синхронизация Vector KB (существующий)
0 3 * * * cd /path/to/lse && python scripts/sync_vector_kb_cron.py >> logs/sync_vector_kb.log 2>&1

# 4. Анализ исходов событий (НОВЫЙ)
0 4 * * * cd /path/to/lse && python scripts/analyze_event_outcomes_cron.py >> logs/analyze_event_outcomes.log 2>&1
```

**Время запуска:**
- 1:00 - Получение новостей
- 2:00 - Добавление sentiment (после получения новостей)
- 3:00 - Синхронизация Vector KB (после sentiment)
- 4:00 - Анализ исходов (для событий, которым уже прошло достаточно времени)

---

## ✅ Проверка результатов

### Проверка исходов событий

```python
from sqlalchemy import create_engine, text
from config_loader import get_database_url
import json

engine = create_engine(get_database_url())
with engine.connect() as conn:
    result = conn.execute(text('''
        SELECT COUNT(*) 
        FROM trade_kb 
        WHERE outcome_json IS NOT NULL
          AND outcome_json::text != 'null'::text
    '''))
    print(f"Событий с исходами: {result.fetchone()[0]}")
```

### Проверка sentiment

```python
from sqlalchemy import create_engine, text
from config_loader import get_database_url

engine = create_engine(get_database_url())
with engine.connect() as conn:
    result = conn.execute(text('''
        SELECT 
            COUNT(*) as total,
            COUNT(sentiment_score) as with_sentiment
        FROM knowledge_base
        WHERE ts >= NOW() - INTERVAL '7 days'
    '''))
    row = result.fetchone()
    print(f"Новостей за 7 дней: {row[0]}")
    print(f"С sentiment: {row[1]} ({row[1]/row[0]*100:.1f}%)")
```

### Проверка влияния на решения

Запустите `boss_dashboard.py` и проверьте блоки "News Impact Analysis":

```bash
python boss_dashboard.py
```

Теперь должны появиться данные о:
- Исторических паттернах (если есть исходы)
- Уверенности в паттернах
- Среднем изменении цены после похожих событий

---

## 🔧 Настройка параметров

### Для анализа исходов

В `config.env` или через переменные окружения:

```bash
# Минимум дней после события для анализа
EVENT_OUTCOME_DAYS_AFTER=7

# Лимит событий за один запуск
EVENT_OUTCOME_LIMIT=100

# Размер батча
EVENT_OUTCOME_BATCH_SIZE=50
```

### Для sentiment анализа

```bash
# Анализировать новости за последние N дней
SENTIMENT_DAYS_BACK=1

# Лимит новостей за один запуск (для экономии LLM ресурсов)
SENTIMENT_LIMIT=50

# Размер батча (меньше для LLM)
SENTIMENT_BATCH_SIZE=10
```

---

## 📊 Ожидаемые результаты

После первоначального запуска:

1. **Исходы событий:**
   - ~10-30% событий получат исходы (зависит от наличия котировок)
   - MACRO/US_MACRO события будут пропущены (нет котировок)

2. **Sentiment анализ:**
   - ~50-100 новостей получат sentiment (зависит от лимита)
   - Покрытие sentiment увеличится с ~0% до ~20-30%

3. **Влияние на решения:**
   - После накопления данных (1-2 недели) система начнет находить паттерны
   - Рекомендации могут изменяться на основе исторических паттернов

---

## 🐛 Устранение проблем

### "Нет котировок для MACRO"

Это нормально - макро-новости не имеют тикеров с котировками. События для MACRO/US_MACRO будут пропущены.

### "USE_LLM=false"

Для sentiment анализа нужен LLM:
1. Установите `USE_LLM=true` в `config.env`
2. Настройте LLM сервис (proxyapi.ru, Groq и т.д.)

### Медленная работа

- Уменьшите `SENTIMENT_LIMIT` для экономии LLM ресурсов
- Уменьшите `EVENT_OUTCOME_LIMIT` для быстрого тестирования
- Используйте меньшие батчи

---

## 📝 Следующие шаги

1. ✅ Запустить анализ исходов для существующих событий
2. ✅ Добавить sentiment к новостям без sentiment
3. ✅ Настроить cron jobs для автоматизации
4. ⏳ Подождать накопления данных (1-2 недели)
5. ⏳ Проверить влияние на решения в `boss_dashboard.py`

---

## 📚 Дополнительная документация

- `docs/NEWS_ANALYSIS_AUTOMATION.md` - Подробная документация по всем скриптам
- `docs/NEWS_IMPACT_TRACKING.md` - Отслеживание влияния новостей на решения
- `docs/BOSS_DASHBOARD_NEWS_IMPACT_SUMMARY.md` - Саммари текущего состояния
