# Интеграция Vector KB в Boss Dashboard

## Обзор

В `boss_dashboard.py` интегрирован функционал Vector Knowledge Base и анализа влияния новостей. Теперь дашборд использует исторические паттерны для улучшения рекомендаций.

## Добавленный функционал

### 1. Анализ новостей

- **`get_recent_news_for_ticker()`** - подсчет новостей за последние 7 дней для тикера
- **`analyze_news_impact_pattern()`** - поиск похожих исторических событий и анализ паттернов

### 2. Новые поля в TickerContext

```python
recent_news_count: int = 0                    # Количество новостей за 7 дней
similar_events_count: int = 0                 # Количество похожих исторических событий
news_impact_pattern: Optional[str] = None      # 'POSITIVE', 'NEGATIVE', 'NEUTRAL'
news_impact_confidence: Optional[float] = None # Уверенность в паттерне (0.0-1.0)
historical_avg_change: Optional[float] = None  # Среднее изменение цены после похожих событий (%)
```

### 3. Интеграция в рекомендации

Функция `select_recommendation()` теперь учитывает исторические паттерны:

- Если похожие события приводили к росту >2% с уверенностью >50%:
  - Рекомендация может быть изменена с `HOLD` на `STRONG_BUY`
  - В reasoning добавляется информация о историческом росте

- Если похожие события приводили к падению >2% с уверенностью >50%:
  - Рекомендация может быть изменена с `STRONG_BUY` на `LIMIT_ORDER`
  - В reasoning добавляется предупреждение об осторожности

### 4. Отображение в дашборде

В выводе каждого тикера добавлен блок "News Analysis":

```
News Analysis: News (7d): 12 | Similar events: 10 | Impact: 📈 POSITIVE (75%) | Avg change: +3.2%
```

## Пример использования

```bash
python boss_dashboard.py
```

Вывод будет включать:

```
----------------------------------------------------------------------------------------------------
SNDK   | Price: $612.22 | VIX: 20.26 (NEUTRAL) | Corr vs MU: 0.85 (In-Sync)
Strategy Selected: MomentumStrategy
Recommendation: STRONG_BUY
Reasoning: VIX нейтральный, корреляция с сектором низкая — бумага идет на собственных драйверах...
Исторический анализ: похожие события приводили к росту 3.2% (уверенность 75%, выборка 10 событий).
News Analysis: News (7d): 12 | Similar events: 10 | Impact: 📈 POSITIVE (75%) | Avg change: +3.2%
```

## Технические детали

### Ленивая инициализация

VectorKB и NewsImpactAnalyzer инициализируются лениво (при первом использовании):

```python
_vector_kb: Optional[VectorKB] = None
_news_analyzer: Optional[NewsImpactAnalyzer] = None

def get_vector_kb() -> VectorKB:
    global _vector_kb
    if _vector_kb is None:
        _vector_kb = VectorKB()
    return _vector_kb
```

Это позволяет избежать загрузки модели при каждом запуске, если анализ новостей не требуется.

### Поиск похожих событий

Алгоритм поиска:

1. Получает последнюю новость для тикера из `knowledge_base`
2. Использует её как контекст для векторного поиска в `trade_kb`
3. Ищет похожие события за последний год (similarity >= 0.4)
4. Анализирует исходы похожих событий через `aggregate_patterns()`

### Обработка ошибок

Все функции анализа новостей имеют обработку ошибок и возвращают безопасные значения по умолчанию:

- При отсутствии новостей: `recent_news_count = 0`
- При отсутствии похожих событий: все поля паттерна = `None`
- При ошибках БД: логирование в debug режиме, продолжение работы

## Производительность

- **Загрузка модели:** ~4-6 секунд при первом использовании (кэшируется)
- **Поиск похожих событий:** ~100-500ms на тикер (зависит от размера БД)
- **Анализ паттернов:** ~10-50ms (если есть данные об исходах)

## Ограничения

1. **Требуются данные об исходах:** Для анализа паттернов нужны данные в `outcome_json` колонке `trade_kb`. Если данных нет, анализ возвращает пустые значения.

2. **Минимальная выборка:** Для надежных выводов рекомендуется минимум 5-10 похожих событий с известными исходами.

3. **Контекст поиска:** Поиск использует последнюю новость из `knowledge_base`. Если новостей нет, используется только тикер как запрос.

## Следующие шаги

1. ✅ Базовая интеграция завершена
2. ⏳ Автоматический анализ исходов для новых событий (через cron)
3. ⏳ Улучшение контекста поиска (использование нескольких последних новостей)
4. ⏳ Кэширование результатов анализа для ускорения работы

## Связанные файлы

- `services/vector_kb.py` - основной модуль Vector KB
- `services/news_impact_analyzer.py` - анализ исходов событий
- `scripts/sync_vector_kb_cron.py` - синхронизация новостей в Vector KB
- `docs/VECTOR_KB_USAGE.md` - документация по Vector KB
