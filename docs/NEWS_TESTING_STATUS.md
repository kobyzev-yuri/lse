# Статус тестирования скриптов новостей

## 📊 Общий статус

| Скрипт | Статус | API ключ | Тестирование |
|--------|--------|----------|--------------|
| **1. RSS фиды** | ⚠️ Ошибка | Не требуется | ❌ Нет модуля feedparser |
| **2. Investing.com** | ⚠️ Ошибка | Не требуется | ❌ Структура HTML изменилась, 429 Too Many Requests |
| **3. Alpha Vantage** | ✅ РАБОТАЕТ | ✅ Добавлен | ✅ 9635 earnings + 232 новости сохранено |
| **4. NewsAPI** | ✅ РАБОТАЕТ | ✅ Добавлен | ✅ 6 новостей сохранено |

## ✅ Выполнено

1. ✅ **Миграция БД** - добавлены поля `event_type`, `region`, `importance`, `link`
2. ✅ **NewsAPI ключ** - добавлен в `config.env`: `NEWSAPI_KEY=00a58c9cb0244f46b1da0e1be857b803`
3. ✅ **Alpha Vantage ключ** - добавлен в `config.env`: `ALPHAVANTAGE_KEY=IMJV61JSCK68IRAE`
4. ✅ **Все модули созданы** - готовы к тестированию

## 🧪 Результаты тестирования (2026-02-19)

### ✅ Успешно работают:

1. **NewsAPI** ✅
   - Получено: 6 уникальных макро-новостей
   - Сохранено в БД: 6 новостей
   - Статус: Работает отлично

2. **Alpha Vantage** ✅
   - Получено: 9635 earnings записей + 50 новостей
   - Сохранено в БД: 9635 earnings + 232 новости (для разных тикеров)
   - Статус: Работает отлично

### ⚠️ Требуют исправления:

1. **RSS фиды** ❌
   - Ошибка: `ModuleNotFoundError: No module named 'feedparser'`
   - Решение: `pip install feedparser>=6.0.10`

2. **Investing.com** ❌
   - Ошибка 1: Не найдена таблица календаря (структура HTML изменилась)
   - Ошибка 2: `429 Too Many Requests` (слишком частые запросы)
   - Решение: Обновить парсер под новую структуру HTML, добавить задержки между запросами

## 🧪 План тестирования по одному

### Тест 1: RSS фиды центральных банков ⏳

**Команда:**
```bash
cd /home/cnn/lse && python3 services/rss_news_fetcher.py
```

**Что проверяем:**
- ✅ Парсинг RSS фидов Fed, BoE, ECB, BoJ
- ✅ Сохранение в БД с правильными полями (event_type, region, importance)
- ✅ Фильтрация дубликатов по link
- ✅ Логирование процесса

**Ожидаемый результат:**
- Новости загружены из всех RSS фидов
- Сохранены в таблицу `knowledge_base`
- Логи показывают количество полученных/сохраненных новостей

**Проверка в БД:**
```sql
SELECT COUNT(*) FROM knowledge_base WHERE event_type LIKE 'FOMC%' OR event_type LIKE 'BOE%' OR event_type LIKE 'ECB%';
SELECT ts, ticker, source, event_type, region, importance FROM knowledge_base WHERE event_type IS NOT NULL ORDER BY ts DESC LIMIT 10;
```

---

### Тест 2: Investing.com Economic Calendar ⏳

**Команда:**
```bash
cd /home/cnn/lse && python3 services/investing_calendar_parser.py
```

**Что проверяем:**
- ✅ Парсинг календаря для всех регионов (USA, UK, EU, Japan, China, Switzerland)
- ✅ Определение типов событий (RATE_DECISION, CPI, NFP, GDP и т.д.)
- ✅ Сохранение с правильными полями
- ✅ Обработка ошибок парсинга

**Ожидаемый результат:**
- События загружены для всех регионов
- Правильно определены event_type и importance
- Сохранены в БД

**Проверка в БД:**
```sql
SELECT region, event_type, COUNT(*) as count 
FROM knowledge_base 
WHERE source LIKE '%Investing.com%' 
GROUP BY region, event_type 
ORDER BY count DESC;
```

---

### Тест 3: NewsAPI ⏳

**Команда:**
```bash
cd /home/cnn/lse && python3 services/newsapi_fetcher.py
```

**Что проверяем:**
- ✅ Подключение к NewsAPI с ключом
- ✅ Получение макро-новостей (Fed, ECB, BoE, экономические индикаторы)
- ✅ Определение региона по контенту
- ✅ Сохранение в БД

**Ожидаемый результат:**
- Новости получены из NewsAPI
- Правильно определены регионы (USA → US_MACRO, EU → MACRO)
- Сохранены в БД

**Проверка в БД:**
```sql
SELECT COUNT(*) FROM knowledge_base WHERE source = 'NewsAPI';
SELECT ticker, region, event_type, LEFT(content, 100) as preview 
FROM knowledge_base 
WHERE source = 'NewsAPI' 
ORDER BY ts DESC 
LIMIT 10;
```

---

### Тест 4: Alpha Vantage ⏳ (требует API ключ)

**Команда:**
```bash
cd /home/cnn/lse && python3 services/alphavantage_fetcher.py
```

**Что проверяем:**
- ✅ Подключение к Alpha Vantage (требует `ALPHAVANTAGE_KEY`)
- ✅ Получение Earnings Calendar
- ✅ Получение News Sentiment
- ✅ Сохранение в БД

**Требуется:**
- ✅ API ключ добавлен: `ALPHAVANTAGE_KEY=IMJV61JSCK68IRAE`

**Ожидаемый результат:**
- Earnings календарь загружен
- Новости с sentiment загружены
- Сохранены в БД

---

### Тест 5: Объединенный скрипт ⏳

**Команда:**
```bash
cd /home/cnn/lse && python3 scripts/fetch_news_cron.py
```

**Что проверяем:**
- ✅ Запуск всех источников последовательно
- ✅ Логирование статуса каждого источника
- ✅ Обработка ошибок (если источник недоступен)
- ✅ Итоговый отчет

**Ожидаемый результат:**
- Все доступные источники отработали
- Логи показывают статус каждого источника
- Новости сохранены в БД

---

## 📝 Чеклист тестирования

- [ ] **Тест 1:** RSS фиды - запустить и проверить результаты
- [ ] **Тест 2:** Investing.com - запустить и проверить результаты
- [ ] **Тест 3:** NewsAPI - запустить и проверить результаты
- [ ] **Тест 4:** Alpha Vantage - получить ключ и протестировать
- [ ] **Тест 5:** Объединенный скрипт - запустить все источники
- [ ] **Проверка БД:** Убедиться что все новости сохранены корректно
- [ ] **Проверка дубликатов:** Убедиться что фильтрация работает
- [ ] **Проверка полей:** Убедиться что event_type, region, importance заполнены

---

## 🔍 Команды для проверки результатов

### Проверка количества новостей по источникам:
```sql
SELECT source, COUNT(*) as count, 
       COUNT(DISTINCT event_type) as event_types,
       MIN(ts) as first_news, 
       MAX(ts) as last_news
FROM knowledge_base 
WHERE ts >= CURRENT_DATE - INTERVAL '7 days'
GROUP BY source 
ORDER BY count DESC;
```

### Проверка новостей по типам событий:
```sql
SELECT event_type, region, COUNT(*) as count
FROM knowledge_base 
WHERE event_type IS NOT NULL
GROUP BY event_type, region
ORDER BY count DESC;
```

### Проверка последних новостей:
```sql
SELECT ts, ticker, source, event_type, region, importance, 
       LEFT(content, 80) as preview
FROM knowledge_base 
WHERE event_type IS NOT NULL
ORDER BY ts DESC 
LIMIT 20;
```

### Проверка дубликатов (не должно быть):
```sql
SELECT link, COUNT(*) as duplicates
FROM knowledge_base 
WHERE link != ''
GROUP BY link 
HAVING COUNT(*) > 1;
```

---

## ⚠️ Известные проблемы

1. **RSS фиды:** Некоторые фиды могут быть недоступны или изменена структура
2. **Investing.com:** Структура HTML может меняться, требуется адаптация парсера
3. **NewsAPI:** Лимит 100 запросов/день на бесплатном tier
4. **Alpha Vantage:** Лимит 5 запросов/минуту, 500/день на бесплатном tier

---

**Последнее обновление:** 2026-02-19  
**Следующий шаг:** Начать тестирование с RSS фидов
